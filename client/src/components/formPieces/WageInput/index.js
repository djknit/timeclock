import React from 'react';

function WageInput({
  name,
  value,
  label,
  sublabel,
  hasProblem,
  helptText,
  handleChange,
  isActive,
  formId,
  type,
  isInline
}) {

  return (
    <fieldset>
      
    </fieldset>
  );
}

export default WageInput;